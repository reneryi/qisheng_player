# Qisheng Player v1.2.10 更新日志

本次发布包含多项歌词、封面旋转、播放进度条的视觉大升级与极致物理交互重构，并对大封面发光进行了关键性能调优。

## 🌟 新特性与交互升级

### 1. 歌词视觉与手势缩放重构
- **逐行高斯模糊景深过渡**：为歌词行引入平滑虚化滤镜。当前播放句完全清晰，已播放历史句微虚（不透明度 `0.32`，模糊 `0.8`），未来未播放句深虚（不透明度 `0.22`，模糊 `1.8`），形成了优雅的杂志级景深聚焦感。同时通过内 Padding 缓冲，彻底规避了高斯模糊渲染边界被硬切断的硬线。
- **被动 Listener 捏合缩放（免冲突）**：废除了原有的 `GestureDetector.onScale` 识别，改由在最外层 `Listener` 被动测距追踪双指的物理坐标变化。在**零干扰歌词列表单指流畅滚动**的前提下，完美实现了手势捏合双指调整字号大小。
- **Ctrl + 鼠标滚轮缩放**：拦截了桌面端的 `PointerScrollEvent` 信号，在按住 `Ctrl` 时滚动鼠标或触控板双指即可无级调节歌词字体大小。
- **毛玻璃字号百分比提示胶囊**：无论通过手势、滚轮还是控制按钮调整字号，顶部会实时淡入浮现毛玻璃圆角指示胶囊（如 `歌词大小: 120%`），操作结束后 1.2 秒平滑淡出。

### 2. 旋转封面物理惯性阻尼与自动回正
- **加速起跑与惯性滑行**：摒弃生硬的瞬停旋转，使用自定义物理 Ticker 实时仿真角速度。播放歌曲时，封面历经 `1.5` 秒平滑物理加速到满速；点击暂停时，封面随摩擦力物理减速滑行 `1.8` 秒直到静止。
- **暂停平滑回正对齐**：当封面旋转完全停下后，会自动触发 `0.8` 秒的 `Curves.easeOutCubic` 缓动动画，将封面顺滑回正对齐至 0 度（正北方向），杜绝视觉杂乱。

### 3. 果冻声波进度条（物理引力与 Preview Tooltip）
- **悬停引力水波形变**：光标在 `WaveformSlider` 上划过或悬停时，会触发物理吸引形变，在光标位置的波形柱子会呈水波状隆起，给用户强烈的探索趣味和人机交互感。
- **毛玻璃时间气泡提示**：在光标悬停或拖动时，正上方实时浮现对应位置的时间预览（如 `02:34`），并加设边界 clamp 安全防护，防止气泡溢出进度条边缘被裁剪。

---

## ⚡ 性能与稳定性调优

- **弥散流光发光能耗调优**：将沉浸式大封面四周 `32` 像素弥散呼吸发光所依赖的 `ImageFiltered` 高斯模糊层移出了 `AnimatedBuilder` 的帧重绘循环。使其在一整首歌曲期间只在 GPU 缓存中静态生成一次，每一帧动画只需做极小消耗的矩阵缩放与透明度变换，**从根本上扫清了由于每一帧进行像素模糊计算导致的主线程滑动卡顿与耗电发热瓶颈。**
